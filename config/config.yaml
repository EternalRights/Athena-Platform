# config/config.yaml
# Web自动化测试平台配置文件

# 浏览器配置
browser:
  name: "chrome"  # 支持 chrome, firefox, edge
  headless: false  # 是否以无头模式运行
  maximize: true   # 是否最大化窗口
  implicit_wait: 10  # 隐式等待时间（秒）
  page_load_timeout: 30  # 页面加载超时时间（秒）

# 测试环境配置
environment:
  base_url: "https://example.com"
  test_data_file: "config/test_data.yaml"
  screenshot_path: "reports/screenshots/"
  log_level: "INFO"

# 动态元素定位配置
element_locator:
  explicit_wait: 20  # 显式等待时间（秒）
  polling_frequency: 0.5  # 轮询频率（秒）
  retry_attempts: 3  # 重试次数
  strategies:
    - "id"
    - "name"
    - "xpath"
    - "css_selector"
    - "class_name"

# 报告配置
report:
  allure_results_path: "reports/allure-results/"
  html_report_path: "reports/html/"
  enable_video: false

# Jenkins集成配置
jenkins:
  enable: true
  pipeline_name: "web-automation-test"
  build_trigger: "SCM"